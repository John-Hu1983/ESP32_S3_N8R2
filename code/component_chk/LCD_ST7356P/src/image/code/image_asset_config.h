#ifndef IMAGE_ASSET_CONFIG_H
#define IMAGE_ASSET_CONFIG_H

/*
 * Legacy config header kept for compatibility.
 * GIF sets are now all compiled in and selected at runtime.
 */
#define IMAGE_GIF_SET 1

#endif
